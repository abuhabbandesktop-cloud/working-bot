# SECURE ENVIRONMENT CONFIGURATION TEMPLATE
# Copy this file to backend/.env and bot/.env and update with SECURE values

# ⚠️  SECURITY WARNING: NEVER commit actual secrets to version control!
# Generate strong random secrets using: openssl rand -base64 32

# BOT TOKEN - Get from @BotFather and keep SECRET
BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# API INGEST SECRET - Generate strong random string
# Must match between bot and backend
API_INGEST_SECRET=GENERATE_STRONG_SECRET_HERE

# JWT SECURITY CONFIGURATION
# Generate cryptographically secure secrets!
JWT_SECRET=GENERATE_STRONG_JWT_SECRET_HERE
JWT_ALG=HS256
ACCESS_TOKEN_TTL_HOURS=1
REFRESH_TOKEN_TTL_DAYS=7

# ADMIN CREDENTIALS - Change these immediately after first login!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# DATABASE AND MEDIA PATHS
SQLITE_PATH=../data/app.db
MEDIA_DIR=../data/media

# BACKEND CONFIGURATION
BACKEND_BASE_URL=http://127.0.0.1:8000

# SECURITY SETTINGS
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# LOGGING CONFIGURATION
LOG_LEVEL=INFO
LOG_FILE=../data/app.log
